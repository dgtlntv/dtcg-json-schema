{
    "name": "dtcg-json-schema",
    "version": "1.0.0",
    "description": "Design Tokens Community Group JSON Schema validator",
    "type": "module",
    "private": true,
    "scripts": {
        "build": "tsc",
        "test": "tsx --test test/*.test.ts test/utils/*.test.ts",
        "test:preprocessors": "tsx --test test/utils/preprocessors.test.ts",
        "test:format-validation": "tsx --test test/format-validation.test.ts",
        "test:resolver-validation": "tsx --test test/resolver-validation.test.ts",
        "compile:format": "ajv compile --spec=draft7 -c ajv-formats --strict=false -r 'schemas/format/values/*.json' -r schemas/format/tokenType.json -r schemas/format/token.json -r schemas/format/group.json -r schemas/format/groupOrToken.json -s schemas/format/format.json",
        "compile:resolver": "ajv compile --spec=draft7 -c ajv-formats --strict=false -r 'schemas/format/values/*.json' -r schemas/format/tokenType.json -r schemas/format/token.json -r schemas/format/group.json -r schemas/format/groupOrToken.json -r schemas/format/format.json -r schemas/resolver/modifier.json -r schemas/resolver/resolutionOrder.json -r schemas/resolver/set.json -s schemas/resolver/resolver.json",
        "validate:format": "ajv validate --spec=draft7 -c ajv-formats --strict=false -r 'schemas/format/values/*.json' -r schemas/format/tokenType.json -r schemas/format/token.json -r schemas/format/group.json -r schemas/format/groupOrToken.json -s schemas/format/format.json -d",
        "validate:resolver": "ajv validate --spec=draft7 -c ajv-formats --strict=false -r 'schemas/format/values/*.json' -r schemas/format/tokenType.json -r schemas/format/token.json -r schemas/format/group.json -r schemas/format/groupOrToken.json -r schemas/format/format.json -r schemas/resolver/modifier.json -r schemas/resolver/resolutionOrder.json -r schemas/resolver/set.json -s schemas/resolver/resolver.json -d"
    },
    "devDependencies": {
        "@types/node": "^24.10.1",
        "ajv": "^8.17.1",
        "ajv-cli": "^5.0.0",
        "ajv-formats": "^3.0.1",
        "tsx": "^4.20.6",
        "typescript": "^5.9.3"
    },
    "keywords": [
        "design-tokens",
        "dtcg",
        "json-schema",
        "validation"
    ]
}
